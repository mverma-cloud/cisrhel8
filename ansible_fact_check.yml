---
  - name: Check ansible facts
    hosts: servera
    become: yes
    become_user: root
    tasks: 
      - name: "Grab lisr of users"
        command: "awk -F: '{print $1}' /etc/passwd"
        args:
          warn: no
        register: users
      - debug: msg="{{ users.stdout_lines}}"
